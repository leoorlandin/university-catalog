{% extends "layout.njk" %}

{% block content %}

  <div class="card">
    <section class="avatar" style="background: url({{student.avatar_url}}) no-repeat center center / cover"></section>

    <section class="details">
      <form method="POST" action="/students?_method=PUT">

        <h3>Detalhes</h3>

        {% include "students/fields.njk" %}

        <input type="hidden" name="id" value="{{student.id}}">
      </form>

      <form action="/students?_method=DELETE" method="POST" id="delete-button">
        <input type="hidden" name="id" value="{{student.id}}">
        <button type="submit">Deletar</button>
      </form>

      {% include "../confirm.njk" %}
    </section>
  </div>

{% endblock content %}